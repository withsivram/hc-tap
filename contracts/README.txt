This project reads simple medical notes, finds Problems (e.g., “asthma”) and Medications (e.g., “metformin”), and saves those results.
Folders:
- fixtures/notes = example notes to test with (fake data)
- fixtures/entities = example outputs (one JSON object per line in a .jsonl file)

